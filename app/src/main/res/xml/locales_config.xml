<?xml version="1.0" encoding="utf-8"?>
<!-- Per-App-Sprache (Android 13+): Die drei unterstuetzten Sprachen erscheinen
     im System-Picker (App-Info > Sprache) und koennen pro App von der
     Systemsprache abweichen — ohne eigenen In-App-Switch. Auf API < 33 wird
     das Attribut ignoriert (minSdk 24 unveraendert). -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="de" />
    <locale android:name="en" />
    <locale android:name="fr" />
</locale-config>
