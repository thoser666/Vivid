# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "gradle" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly"
  # Pinned Actions automatisch per PR aktualisieren (siehe RELEASE.md:
  # Abschnitt 'CI-Haertung: Pinned Actions' -> Update-Fahrplan).
  # Dependabot liest die `# vX`-Provenienz-Kommentare und zieht SHA + Kommentar gemeinsam.
  - package-ecosystem: "github-actions"
    directory: "/" # .github/workflows wird standardmaessig gescannt
    schedule:
      interval: "weekly"
